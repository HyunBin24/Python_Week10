# Python_Week10
파이썬 프로그래밍 실습 10주차 과제 (2401334 정현빈)

<br>

## 과제 25
#### 1. 키를 input().split()으로 입력을 받았습니다.
#### 2. 값은 문자형이 아니라 숫자형이라 map(int, input( ).split( ))으로 받았습니다.
#### 3. dict(zip([keys], [values]))을 이용해 딕셔너리로 만들어서 출력했습니다.  
-> zip에는 키와 값은 리스트, 튜플 형태 외에도 iterable(반복 가능한) 객체 모두 가능하다고 해서 map을 이용해 입력받은 value를 따로 리스트로 변환하지 않았습니다.
#### 4. 과제 이미지에서 'alpha' : 30, 'delta' : 60은 출력되어 있지 않아서 del을 이용해 삭제 한 버전도 출력했습니다. 

<br>

## 과제 26
#### 1. 문제에서 요구되는 딕셔너리를 { }을 이용해서 선언했습니다.
#### 2. 그 후 [ ]로 key인 english와 science 접근하여 값을 출력했습니다.

<br>

## 과제 27
#### 1. 문제에서 요구되는 딕셔너리를 { }를 이용해서 선언했습니다.
#### 2. 모든 값을 100으로 변경하기 위해 dict.fromkeys( )을 이용했습니다.
-> dict.fromekeys(iterable, value) 형태이고, dict.keys( )는 반복 가능한 dict_keys 객체를 반환한다고 하여 dict.fromkeys(kim.keys(), 100)을 이용했습니다.
#### 3. 딕셔너리 컴프리헨션을 이용한 방법도 있습니다.

<br>

## 과제 28
#### 1. 문제에서 요구되는 딕셔너리를 { }를 이용해서 선언했습니다.
#### 2. if 문과 in 연산자를 이용해서 딕셔너리에 english 키가 있는지 확인했습니다.
#### 3. pop(키)를 이용해 key-value를 삭제 후 출력했습니다.

<br>

## 과제 29
#### 1. 문제에서 요구되는 딕셔너리를 { }를 이용해서 선언했습니다.
#### 2. dict.keys( )와 dict.values( ), dict.items( )를 이용해 key, value, key-value를 출력했습니다.

<br>

## 과제 30
#### 1. 문제에서 요구되는 딕셔너리를 { }를 이용해서 선언했습니다.
#### 2. for 문에서 dict.items( )를 이용해 key-value 쌍을 구하여 key, value를 하나씩 꺼냈습니다.
#### 3. if 문을 통해 점수가 90점 이상인 경우에 key만 딕셔너리 만들어서 출력했습니다.

<br>

## 과제 31
#### 1. 문제에서 요구되는 딕셔너리를 { }를 이용해서 선언했습니다.
#### 2. dict.values( )와 sum( )을 이용해 모든 값들의 합을 구했습니다.
#### 3. 그 후 과목 수를 구하기 위해 len( )을 이용하여 평균을 출력했습니다.